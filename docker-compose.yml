services:
  plaseg-api-postgres:
    container_name: plaseg-api-postgres
    image: bitnami/postgresql
    ports:
      - 15432:5432
    environment:
      POSTGRES_USER: plaseg
      POSTGRES_PASSWORD: plaseg
      POSTGRES_DB: plaseg-api
